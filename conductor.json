{
    "scripts": {
        "setup": "cp \"$CONDUCTOR_ROOT_PATH/.env\" .env 2>/dev/null || true",
        "archive": "xcrun simctl delete $(cat .context/simulator-udid.txt) 2>/dev/null || true"
    }
}
